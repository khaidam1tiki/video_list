<button
  class="{{shape + '-button'}} johnny-button"
  loading="{{loading}}"
  type="{{type}}"
  disabled="{{disabled}}"
  style="{{style}}"
>
  <text style="{{titleStyle}}" >{{title}}</text>
</button>