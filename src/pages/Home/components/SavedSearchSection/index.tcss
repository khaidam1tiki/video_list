.saved-search-section-container {
  padding: 12px 16px;
  background-color: var(--white);
}

.saved-search-scrollview {
  display: flex;
  flex-direction: row;
  margin-right: -16px;
  margin-top: 12px;
  margin-left: -16px;
  padding-left: 16px;
}

.saved-search-scrollview::-webkit-scrollbar {
  display: none;
}